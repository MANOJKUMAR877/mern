{
  "name": "mern",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start-all": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm start\" ",
    "install-all": "concurrently \"cd backend && npm install\" \"cd home && npm install\" \"cd dashboard && npm install\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.1",
    "npm-run-all": "^4.1.5"
  }
}
